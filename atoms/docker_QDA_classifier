FROM python:3
WORKDIR /root

# Installs google cloud sdk, this is mostly for using gsutil to export model.
RUN pip install scikit-learn==0.21.3 && \
    pip install cloudml-hypertune && \
    pip install pandas==0.23.4 && \
    pip install pyYAML==5.1.2 && \
    wget -nv \
    https://dl.google.com/dl/cloudsdk/release/google-cloud-sdk.tar.gz && \
    mkdir /root/tools && \
    tar xvzf google-cloud-sdk.tar.gz -C /root/tools && \
    rm google-cloud-sdk.tar.gz && \
    /root/tools/google-cloud-sdk/install.sh --usage-reporting=false \
        --path-update=false --bash-completion=false \
        --disable-installation-options && \
    rm -rf /root/.config/* && \
    ln -s /root/.config /config && \
    # Remove the backup directory that gcloud creates
    rm -rf /root/tools/google-cloud-sdk/.install/.backup && \
    echo '[GoogleCompute]\nservice_account = default' > /etc/boto.cfg && \
    mkdir /root/mlatoms && \
    apt-get clean

# Path configuration
ENV PATH $PATH:/root/tools/google-cloud-sdk/bin

# Copies the trainer code
COPY entrypoint_QDA_classifier.py /root/mlatoms/entrypoint_QDA_classifier.py
COPY trainer.py /root/mlatoms/trainer.py

# Sets up the entry point to invoke the trainer.
ENTRYPOINT ["python3", "mlatoms/entrypoint_QDA_classifier.py"]